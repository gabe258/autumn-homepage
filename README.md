# Autumn — homepage

A single static homepage. No framework, no build step.

- `index.html` — the whole page.
- `img/` — photography and the door sequence. Every image is generated; every property, person and number is invented.

Open `index.html`, or serve the folder with any static host.

Live: https://gabe258.github.io/autumn-homepage/
